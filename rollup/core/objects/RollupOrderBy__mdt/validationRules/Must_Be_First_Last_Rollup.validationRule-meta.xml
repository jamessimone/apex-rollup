<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Must_Be_First_Last_Rollup</fullName>
    <active>true</active>
    <description>Prevents saving a Rollup Order By record if the parent rollup&apos;s operation isn&apos;t set to FIRST/LAST</description>
    <errorConditionFormula>NOT(
  OR(
    ISPICKVAL(Rollup__r.RollupOperation__c,&apos;FIRST&apos;),
    ISPICKVAL(Rollup__r.RollupOperation__c, &apos;LAST&apos;)
  )
)</errorConditionFormula>
    <errorMessage>Parent rollup operation must be properly configured as FIRST/LAST</errorMessage>
</ValidationRule>
